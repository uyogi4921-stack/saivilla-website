services:
  - type: web
    name: saivilla-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGO_URL
        value: mongodb+srv://uyogi4921_db_user:8Qi4Cl2ZwgXqGG5h@cluster0.91eammn.mongodb.net/saivilla_db?retryWrites=true&w=majority&appName=Cluster0
      - key: DB_NAME
        value: saivilla_db
      - key: CORS_ORIGINS
        value: "*"
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        value: saivillaoffice@gmail.com
      - key: SMTP_PASSWORD
        value: Saivilla@15
      - key: NOTIFICATION_EMAIL
        value: saivillaoffice@gmail.com
      # Username is not a secret; the password is what gates access.
      - key: ADMIN_USERNAME
        value: sureshyogi
      # Set in the Render dashboard — never commit the admin password.
      - key: ADMIN_PASSWORD
        sync: false
      - key: ADMIN_TOKEN_SECRET
        generateValue: true
